// This is a basic Flutter widget test.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Default widget test', (WidgetTester tester) async {
    const mockInteger = 1;
    expect(1, mockInteger);
  });
}
